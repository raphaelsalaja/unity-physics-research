using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;

public class Signboard : MonoBehaviour
{
    /*
    [SerializeField] private Typewriter textCanvasToType;
    [SerializeField] private Text respectiveCanvasLabel;
    [SerializeField] private StringsToType stringsToType;
    

    enum StringsToType
    {
      SapceToJump,
      ControlToDash,
      SpaceToWallJump,
      ShiftToSprint
    }

    public void Run()
    {
        string s;
        switch (stringsToType)
        {
            case StringsToType.SapceToJump:
                s ="Press space to jump over this gap\nthe longer you hold jump\nthe higher you will go";
                break;
            case StringsToType.ControlToDash:
                s = "press the control key to dash to make this gap";
                break;
            case StringsToType.SpaceToWallJump:
                s = "press the space key when making contact with the wall to wall jump";
                break;
            case StringsToType.ShiftToSprint:
                s = "hold the shift key while moving to sprint to clear even larger gaps";
                break;
            default:
                break;
        }

        textCanvasToType.Run(s, respectiveCanvasLabel);
    }
*/
}
